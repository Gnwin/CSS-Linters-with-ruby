#!/usr/bin/env ruby
puts 'hello'

# brackets
# curly braces
# white spaces in variable names
# incomplete strings '' ""

# File.foreach('bin/lint').with_index do |line, line_num|
#   puts "#{line_num}: #{line}"
# end
