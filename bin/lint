#!/usr/bin/env ruby
puts 'hello'

# brackets
# curly braces
# white spaces in variable names
# incomplete strings '' ""

# File.foreach('bin/lint').with_index do |line, line_num|
#   puts "#{line_num}: #{line}"
# end

user_file = ARGV
# user_file = user_file.shift
# puts user_file.to_s
# if user_file.to_s.empty?

puts user_file

